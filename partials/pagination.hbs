<nav class="w-full font-sans font-light" role="navigation">
  {{#if prev}}
  <a class="text-carpeaqua-blue" href="{{page_url prev}}">&larr; Newer Posts</a>
  <span class="px-5">Page {{page}} of {{pages}}</span>
  {{/if}}
  {{#if next}}
  <a class="text-carpeaqua-blue" href="{{page_url next}}">Older Posts &rarr;</a>
  {{/if}}
</nav>